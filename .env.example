# Server configuration
PORT=5000
NODE_ENV=development

# Database configuration - Local development
DB_NAME=personal_finance
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# Database configuration - Production (Railway)
# Uncomment this line in production or when deploying to Railway
# DATABASE_URL=postgres://username:password@host.railway.app:5432/database

# JWT configuration
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=90d
JWT_COOKIE_EXPIRES_IN=90
